# Plan: Client Preview Testing

- [ ] **Step 1: Setup Environment**
  - [ ] Navigate to `client/` directory.
  - [ ] Ensure dependencies are installed (`npm install` or `npm ci`).

- [ ] **Step 2: Build Application**
  - [ ] Run `npm run build`.
  - [ ] Verify build artifacts exist (check `dist/` or `build/` folder).

- [ ] **Step 3: Start Preview Server**
  - [ ] Run `npm run preview`.
  - [ ] Note the local URL (usually `http://localhost:4173`).

- [ ] **Step 4: Smoke Test**
  - [ ] Verify the application loads at the preview URL.
  - [ ] Check for any console errors in the browser/terminal.