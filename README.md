# AlfaVoice

Голосовой ассистент с транскрипцией речи в реальном времени.

## Обзор

AlfaVoice - это десктопное приложение для транскрипции речи в текст в реальном времени. Приложение состоит из двух частей:

- **Client**: SvelteKit + Tauri десктопное приложение
- **Server**: Rust + Axum сервер для обработки аудио

## Структура проекта

```
AlfaVoice/
├── client/                 # Клиентское приложение (SvelteKit + Tauri)
│   ├── src/
│   │   ├── lib/
│   │   │   ├── audio.ts   # Модуль захвата аудио
│   │   │   └── socket.ts  # Модуль WebSocket
│   │   └── routes/
│   │       ├── +layout.svelte
│   │       └── +page.svelte
│   ├── src-tauri/         # Tauri backend
│   │   └── src/
│   │       ├── main.rs
│   │       ├── hotkey.rs
│   │       └── audio.rs
│   └── package.json
├── server/                # Серверное приложение (Rust + Axum)
│   ├── src/
│   │   ├── main.rs
│   │   ├── ws.rs
│   │   └── state.rs
│   └── Cargo.toml
└── README.md
```

## Возможности

- 🎤 Запись аудио с микрофона
- 📊 Визуализация уровня громкости
- 🔌 WebSocket соединение с сервером
- ⌨️ Глобальные горячие клавиши (Ctrl+Shift+R)
- 🌙 Темная/светлая тема
- 📝 Транскрипция в реальном времени
- 🔄 Автоматическое переподключение

## Требования

### Для клиента
- Node.js 18+
- npm или yarn
- Rust (для сборки Tauri)
- Системные зависимости для Tauri

### Для сервера
- Rust 1.70+

## Установка и запуск

### 1. Запуск сервера

```bash
cd server
cargo run
```

Сервер запустится на `http://localhost:8080`

### 2. Запуск клиента

```bash
cd client
npm install
npm run dev
```

Клиент запустится на `http://localhost:5173`

### 3. Запуск Tauri приложения

```bash
cd client
npm install
npm run tauri:dev
```

## Использование

1. Запустите сервер: `cd server && cargo run`
2. Запустите клиент: `cd client && npm run tauri:dev`
3. Нажмите кнопку записи или используйте горячую клавишу `Ctrl+Shift+R`
4. Говорите в микрофон
5. Транскрипция появится в реальном времени

## Конфигурация

### Переменные окружения клиента

Создайте файл `client/.env`:

```env
VITE_WS_URL=ws://localhost:8080/ws
VITE_API_URL=http://localhost:8080
NODE_ENV=development
```

### Конфигурация Tauri

Файл `client/src-tauri/tauri.conf.json` содержит настройки приложения:
- Размер окна
- Горячие клавиши
- Путь к иконкам

## Разработка

### Клиент

```bash
cd client

# Установка зависимостей
npm install

# Режим разработки
npm run dev

# Проверка типов
npm run check

# Сборка
npm run build

# Tauri режим разработки
npm run tauri:dev

# Сборка Tauri приложения
npm run tauri:build
```

### Сервер

```bash
cd server

# Режим разработки
cargo run

# Проверка
cargo check

# Сборка
cargo build --release

# Запуск тестов
cargo test
```

## Лицензия

MIT
