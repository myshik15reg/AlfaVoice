# AlfaVoice: Полная техническая документация

## 📋 Содержание
- [Обзор проекта](#обзор-проекта)
- [Архитектура системы](#архитектура-системы)
- [Технические требования](#технические-требования)
- [Функциональные требования](#функциональные-требования)
- [Бизнес-процессы](#бизнес-процессы)
- [Модели ИИ](#модели-искусственного-интеллекта)
- [Структура проекта](#структура-проекта)
- [Инструкция по развертыванию](#инструкция-по-развертыванию)
- [API документация](#api-документация)
- [Конфигурация](#конфигурация)
- [Тестирование](#тестирование)
- [Мониторинг и логирование](#мониторинг-и-логирование)
- [Безопасность](#безопасность)
- [Производительность](#производительность)
- [Масштабирование](#масштабирование)

## Обзор проекта

### Название и краткое описание
**AlfaVoice** — это кроссплатформенный голосовой ассистент корпоративного уровня, аналогичный Wispr Flow, предназначенный для транскрипции речи в текст с последующей вставкой в активные окна приложений. Система работает в реальном времени, поддерживает русский и английский языки, и оптимизирована для работы на CPU-only серверах.

### Цели и задачи
1. **Транскрипция речи**: Преобразование аудио в текст с высокой точностью
2. **Постобработка**: Улучшение текста с помощью LLM-моделей
3. **Вставка в приложения**: Эмуляция ввода текста в активные окна
4. **Офлайн-режим**: Работа без подключения к серверу с последующей синхронизацией
5. **Корпоративная безопасность**: Локальная обработка данных без отправки в облако

### Целевая аудитория
- **Корпоративные пользователи**: Сотрудники компаний, использующие Windows Server 2019
- **Разработчики**: Программисты, ведущие голосовой ввод кода и документации
- **Менеджеры**: Руководители, проводящие совещания и диктующие отчеты
- **Пользователи с ограниченными возможностями**: Люди, предпочитающие голосовой ввод

## Архитектура системы

### Общая архитектура (клиент-сервер)
```
┌─────────────────┐    WebSocket/HTTP    ┌─────────────────┐
│   Клиент Tauri  │◄──────────────────► │  Сервер Rust    │
│                 │                     │                 │
│ • Горячие клавиши│                     │ • Whisper large-v3│
│ • Динамическая   │                     │ • Qwen2-7B      │
│   иконка записи  │                     │ • PostgreSQL    │
│ • Офлайн-очередь │                     │ • AVX-512/AMX   │
└─────────────────┘                     └─────────────────┘
                               ┌─────────────────┐
                               │ Балансировщик   │
                               │     Nginx       │
                               └─────────────────┘
```

### Компоненты сервера
- **WebSocket сервер**: Обработка соединений от клиентов
- **Модуль транскрипции**: Интеграция с whisper.cpp
- **Модуль постобработки**: Работа с Qwen2-7B и Phi-3-mini
- **База данных**: PostgreSQL для истории и настроек
- **Система очередей**: Обработка запросов в порядке приоритета
- **Мониторинг**: Сбор метрик производительности

### Компоненты клиента
- **Интерфейс Svelte**: Настройки, история, управление записью
- **Модуль аудио**: Захват микрофона через Web Audio API
- **Горячие клавиши**: Глобальные хуки клавиатуры (Windows)
- **Локальная БД**: SQLite с шифрованием для офлайн-режима
- **Синхронизация**: Фоновый обмен данными с сервером

### Технологии и фреймворки
| Компонент | Технология | Версия |
|-----------|------------|--------|
| Сервер | Rust | 1.78+ |
| Клиент UI | Svelte | 4.x |
| Клиент фреймворк | Tauri | 1.5+ |
| База данных | PostgreSQL | 15+ |
| Балансировщик | Nginx | 1.24+ |
| Модели ИИ | whisper.cpp + llama.cpp | latest |
| Аудио | Web Audio API | - |

## Технические требования

### Системные требования
- **Сервер**:
  - ОС: Windows Server 2019 / Ubuntu 22.04 / CentOS 8+
  - CPU: Intel Xeon Platinum 8458P (44 ядра, AVX-512, AMX)
  - RAM: 1.5 ТБ (для 50–200 пользователей)
  - Диск: SSD 1 ТБ+
- **Клиент**:
  - ОС: Windows 10/11, Server 2019, macOS 12+, Linux
  - CPU: 2+ ядра
  - RAM: 4 ГБ
  - Аудио: Микрофон

### Аппаратные требования
- **CPU**: Intel Xeon Platinum 8458P 4-го поколения (Sapphire Rapids) с поддержкой AMX
- **Память**: DDR5 для максимальной производительности
- **NUMA архитектура**: 2 сокета для оптимального распределения памяти
- **Сеть**: Gigabit Ethernet для передачи аудио-данных

### Программные зависимости
- **Сборка**:
  - Rust 1.78+
  - Node.js 18+
  - CMake 3.20+
  - Visual Studio 2022 (Windows)
- **Runtime**:
  - .NET 8 Runtime (Windows)
  - libsndfile (Linux)

## Функциональные требования

### Основные режимы работы
1. **После записи**: Полная обработка после остановки записи
2. **По паузе**: Обработка по обнаружении паузы в речи
3. **Быстро**: Использование medium-модели для скорости
4. **Качественно**: Использование large-модели для точности

### Качество и производительность
- **Точность транскрипции**: >95% для clean speech
- **Задержка**: 2–10 сек в зависимости от режима
- **Пропускная способность**: 50–200 одновременных пользователей
- **Параллельные сессии**: 50–80 одновременных сессий
- **Время работы**: 99.9% uptime

### Дополнительные функции
- **Офлайн-режим**: Работа без подключения к серверу
- **История запросов**: Хранение последних N транскрипций
- **Поиск по истории**: Полнотекстовый поиск с PostgreSQL
- **Темная/светлая тема**: Адаптивный интерфейс
- **Горячие клавиши**: Глобальные комбинации клавиш
- **Авторизация**: JWT с refresh tokens

## Бизнес-процессы

### Основной процесс работы
1. Пользователь нажимает горячие клавиши (Ctrl+Win)
2. Клиент захватывает аудио с микрофона
3. Аудио отправляется на сервер по WebSocket
4. Сервер транскрибирует речь с помощью Whisper
5. LLM-модель улучшает текст (пунктуация, форматирование)
6. Результат отправляется обратно клиенту
7. Клиент вставляет текст в активное окно

### Офлайн-процесс
1. При отсутствии соединения аудио сохраняется локально
2. Клиент показывает индикатор "Офлайн"
3. При восстановлении связи запускается синхронизация
4. Записи отправляются на сервер в порядке очереди
5. Результаты добавляются в историю

### Процесс масштабирования
1. Мониторинг показывает загрузку >80%
2. Добавляется новый сервер в балансировщик
3. Сервер конфигурируется с теми же моделями
4. Nginx распределяет нагрузку
5. Трафик перенаправляется на новый сервер

## Модели ИИ

### Whisper модели
- **Основная**: `ggml-large-v3-Q5_K_M.gguf`
- **Быстрая**: `ggml-medium-Q5_K_M.gguf`
- **Оптимизация**: AVX-512 + OpenMP
- **Формат аудио**: 16kHz mono PCM

### LLM модели
- **Основная**: `Qwen2-7B-Instruct-Q5_K_M.gguf`
- **Быстрая**: `Phi-3-mini-4k-instruct-Q5_K_M.gguf`
- **Оптимизация**: AMX + AVX-512
- **Роль**: Постобработка текста (пунктуация, форматирование)

### Оптимизация производительности
- **Сборка**: Кастомная с AMX и AVX-512
- **Квантование**: Q5_K_M для баланса качества/скорости
- **Потоки**: 8–16 на инстанс модели
- **Память**: mmap для эффективного использования RAM

## Структура проекта

```
alfavoice/
├── client/                     # Tauri-клиент
│   ├── src/
│   │   ├── config/
│   │   │   └── ui.config.ts    # Конфигурация UI
│   │   ├── lib/
│   │   │   ├── auth.ts         # Авторизация
│   │   │   ├── audio.ts        # Захват аудио
│   │   │   ├── history.ts      # История транскрипций
│   │   │   ├── sync.ts         # Синхронизация с сервером
│   │   │   ├── theme.ts        # Управление темами
│   │   │   └── hotkeys.ts      # Горячие клавиши
│   │   ├── stores/
│   │   │   ├── authStore.ts    # Состояние авторизации
│   │   │   ├── recordingStore.ts # Состояние записи
│   │   │   └── settingsStore.ts  # Настройки приложения
│   │   ├── components/
│   │   │   ├── RecordingIndicator.svelte # Динамическая иконка
│   │   │   ├── Settings.svelte   # Настройки
│   │   │   ├── History.svelte    # История
│   │   │   └── QueueStatus.svelte # Статус очереди
│   │   ├── App.svelte
│   │   └── main.ts
│   ├── public/
│   │   └── icons/
│   │       ├── icon.ico        # Windows
│   │       ├── icon.icns       # macOS
│   │       └── icon-32.png     # Linux
│   └── src-tauri/
│       ├── src/
│       │   ├── commands/       # Tauri команды
│       │   ├── hotkey_listener.rs # Глобальные хоткеи
│       │   ├── background.rs   # Фоновые процессы
│       │   └── main.rs         # Точка входа
│       ├── icons/              # Иконки приложения
│       ├── tauri.conf.json     # Конфигурация Tauri
│       └── Cargo.toml         # Зависимости Rust
├── server/                     # Rust-сервер
│   ├── src/
│   │   ├── auth/               # Авторизация
│   │   ├── transcription/      # Транскрипция и LLM
│   │   ├── history/            # История
│   │   ├── infrastructure/     # База данных
│   │   ├── domain/             # Бизнес-логика
│   │   ├── interfaces/         # WebSocket/HTTP
│   │   └── main.rs             # Точка входа
│   ├── models/                 # Модели ИИ (gitignored)
│   └── Cargo.toml             # Зависимости
├── scripts/                    # Скрипты сборки
│   ├── build_amx.sh           # Сборка с AMX
│   ├── download_models.sh     # Скачивание моделей
│   ├── setup_os.sh           # Настройка ОС
│   └── load_test.js          # Нагрузочное тестирование
├── docker/                     # Docker
│   └── Dockerfile.amx         # Оптимизированный образ
├── nginx/                      # Балансировщик
│   └── nginx.conf             # Конфигурация
├── docs/                       # Документация
│   ├── API.md                 # API документация
│   ├── DEPLOYMENT.md          # Развертывание
│   ├── ARCHITECTURE.md        # Архитектура
└── README.md                   # Основной README
```

## Инструкция по развертыванию

### Предварительные требования
1. **Установите зависимости**:
   ```bash
   # Ubuntu/Debian
   sudo apt install build-essential cmake git libsndfile1

   # Windows
   # Установите Visual Studio 2022 с C++ build tools
   ```

2. **Установите Rust**:
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   source ~/.cargo/env
   ```

3. **Установите Node.js**:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt install nodejs
   ```

### Шаг 1: Клонирование и настройка
```bash
git clone https://github.com/your-org/alfavoice.git
cd alfavoice
```

### Шаг 2: Скачивание моделей
```bash
./scripts/download_models.sh
```

### Шаг 3: Сборка сервера с AMX
```bash
cd server
../scripts/build_amx.sh
cargo build --release
```

### Шаг 4: Настройка базы данных
```bash
# Создайте базу PostgreSQL
createdb alfavoice

# Выполните миграции
cd server
sqlx migrate run
```

### Шаг 5: Запуск сервера
```bash
cd server
cargo run --release
# Сервер доступен на ws://localhost:3000
```

### Шаг 6: Сборка клиента
```bash
cd client
npm install
npm run tauri build
```

### Шаг 7: Настройка балансировщика (для нескольких серверов)
```bash
# Скопируйте конфигурацию
cp nginx/nginx.conf /etc/nginx/sites-available/alfavoice
ln -s /etc/nginx/sites-available/alfavoice /etc/nginx/sites-enabled/
nginx -t && nginx -s reload
```

## API документация

### WebSocket API
```typescript
// Подключение
const ws = new WebSocket('ws://localhost:3000/transcribe');

// Отправка аудио-чанков
ws.send(JSON.stringify({
  type: 'audio_chunk',
  session_id: 'user123',
  quality: 'high', // 'high' | 'normal'
  mode: 'on_pause', // 'on_pause' | 'after_recording'
  audio: 'base64_pcm_data'
}));

// Получение результатов
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'partial_text') {
    console.log('Текст:', data.text);
    // Вставка в активное окно
  }
};
```

### HTTP API (для истории и настроек)
```typescript
// Получение истории
const history = await fetch('/api/history?limit=5', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// Сохранение настроек
await fetch('/api/settings', {
  method: 'POST',
  headers: { 'Authorization': `Bearer ${token}` },
  body: JSON.stringify({
    theme: 'dark',
    history_limit: 10,
    hotkeys: ['Ctrl', 'Win']
  })
});
```

## Конфигурация

### Сервер (server/.env)
```env
DATABASE_URL=postgresql://user:pass@localhost:5432/alfavoice
JWT_SECRET=your_super_secret_key_here
REDIS_URL=redis://localhost:6379
WHISPER_THREADS=16
LLM_THREADS=16
MAX_QUEUE_SIZE=1000
# NUMA и CPU affinity настройки
NUMA_NODE_0_CORES=0-21
NUMA_NODE_1_CORES=22-43
MEMORY_AFFINITY=1
```

### Клиент (client/src-tauri/tauri.conf.json)
```json
{
  "build": {
    "beforeBuildCommand": "npm run build",
    "devPath": "http://localhost:1420"
  },
  "tauri": {
    "bundle": {
      "active": true,
      "targets": "all",
      "identifier": "com.alfavoice.app",
      "icon": ["icons/icon.ico"]
    },
    "systemTray": {
      "iconPath": "icons/icon-32.png"
    }
  }
}
```

## Тестирование

### Нагрузочное тестирование
```bash
# Запустите k6
./scripts/load_test.js

# Параметры:
# - 50-200 виртуальных пользователей
# - Длительность: 30 секунд
# - Целевой URL: ws://localhost:3000
```

### Модульные тесты
```bash
cd server
cargo test

cd client
npm test
```

### Интеграционные тесты
```bash
# Тестирование WebSocket соединения
npm run test:integration

# Тестирование офлайн-режима
npm run test:offline
```

## Мониторинг и логирование

### Метрики Prometheus
- `whisper_processing_time_seconds`
- `llm_processing_time_seconds`
- `queue_size`
- `active_connections`
- `numa_memory_usage`

### Логирование
- **Сервер**: Structured logging с `tracing` crate
- **Клиент**: Console + файл логов
- **Уровни**: DEBUG, INFO, WARN, ERROR

### Мониторинг
- **Grafana Dashboard**: CPU, RAM, очередь, ошибки
- **Alerting**: При CPU > 80% или ошибках > 5%

## Безопасность

### Авторизация
- **JWT токены** с refresh mechanism
- **Пароли** хешированы Argon2
- **Rate limiting** на API endpoints

### Шифрование
- **Локальная БД**: SQLCipher (AES-256)
- **Передача данных**: TLS 1.3
- **Хранение токенов**: Tauri Secure Storage

### Защита от атак
- **CSRF**: Не требуется (WebSocket)
- **XSS**: Экранирование в UI
- **SQL Injection**: Parameterized queries

## Производительность

### Оптимизация под Intel Xeon Platinum 8458P
- **AVX-512**: +40–60% скорости Whisper
- **AMX**: +300–600% скорости LLM
- **NUMA-aware распределение**: Оптимальное использование памяти
- **CPU affinity**: Ядра 0-21 и 22-43 для разных задач
- **OpenMP**: Параллельная обработка
- **Memory mapping**: Эффективное использование RAM

### Бенчмарки
| Сценарий | Время обработки | RAM | CPU |
|----------|----------------|-----|-----|
| 30 сек речи (high quality) | 2–3 сек | 25 ГБ | 40% |
| 30 сек речи (normal quality) | 1–2 сек | 15 ГБ | 25% |
| 200 пользователей | - | 500 ГБ | 60% |
| NUMA-оптимизация | -25% latency | -30% memory usage | -15% CPU |

## Масштабирование

### Горизонтальное масштабирование
1. **Добавление серверов**: 1cde08, 1cdev09, server03, ...
2. **Балансировка**: Nginx upstream
3. **База данных**: PostgreSQL cluster или replica
4. **Кэширование**: Redis cluster

### Вертикальное масштабирование
1. **CPU**: Увеличение до 88 ядер (2×44)
2. **RAM**: До 1.5 ТБ для максимальной нагрузки
3. **Оптимизация**: Кастомная сборка с AMX

### Автоматическое масштабирование
- **Мониторинг**: CPU > 70% → новый инстанс
- **Балансировка**: Weighted round-robin
- **Failover**: Автоматическое переключение при сбое

---

*Этот документ служит источником истины для всех участников разработки AlfaVoice.*